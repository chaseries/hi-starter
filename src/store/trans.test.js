import { state, mutations, actions, getters } from "./store";

const stateFactory = () => {
  return {
    transType: "loading",
    isInitialLoad: false
  };
};


describe("state", () => {
  test("", () => {

  });
});

describe("mutations", () => {
  test("", () => {
  });
});

describe("actions", () => {
  test("", () => {
    
  });
});

describe("getters", () => {
  test("", () => {

  });
});
